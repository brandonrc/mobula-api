# Changelog

This repo is generated. The spec (`openapi.json`/`openapi.yaml`) is pushed
from [mobula](https://github.com/brandonrc/mobula) CI on API changes; the
`@brandonrc/mobula-client` SDK is regenerated and published from it. Notable
API changes will be summarized here as versioning firms up.
